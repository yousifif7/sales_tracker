{{ $bodyText }}

--
{{ $signatureName }}
{{ $signatureTitle }}
@if (filled($signatureWebsite))
{{ $signatureWebsite }}
@endif
@if (filled($signatureEmail))
{{ $signatureEmail }}
@endif

Reply to this email if useful.
